---
applications:
  - name: ((app-name))
    health-check-type: ((health-check-type))
    buildpacks:
      - https://github.com/cloudfoundry/java-buildpack.git
    services:
      - ((service-name))
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: ((java-version)) }}'